allow init same_process_hal_file:file execute;

allow init adsprpcd_file:file mounton;

# AxionOS kernel tunings - OEM labeling adjustments
# Allow init to access VM dirty writeback settings with different OEM labels
allow init proc_vm_dirty:file rw_file_perms;
allow init proc_dirty_ratio:file rw_file_perms;
